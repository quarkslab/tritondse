#!/usr/bin/env python
# -*- coding: utf-8 -*-

from tritondse.processState import ProcessState


def rtn_exit(pstate : ProcessState):
    raise(Exception('todo'))


def rtn_puts(pstate : ProcessState):
    raise(Exception('todo'))


def rtn_libc_start_main(pstate : ProcessState):
    raise(Exception('todo'))
